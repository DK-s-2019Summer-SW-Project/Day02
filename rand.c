// 가위 바위 보 게임을 만들어 보자
// 셋 중에 하나를 입력, 상대 선수는 컴퓨터로, rand 함수를 돌려서 결과 값을 도출 할 것
#include<stdio.h>
#include<stdlib.h>
#define _CRT_SECURE_NO_WARNNINGS

int main()
{
	/*printf("Starting the rock, ciser, paper");

	int insert;
	char r, c, p;
	int computer, user;
	r = 0;
	c = 1;
	p = 2;

	printf("Insert a symbol : ");
	scanf_s("%d", &insert);

	computer = rand() % 3;
	scanf_s("%d", &user);

	if (computer == user)
	{
		printf("Draw!\n");
	}
	else if (computer < user)
	{
		printf("The user wins!\n");
	}
	else if (computer > user)
	{
		printf("computer wins!\n");
	}

	return 0;*/

	

}